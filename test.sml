val a = 4;
val b = -3;
val c = (2, 3);
val unit = ();
fun w x = while x do 3;

fun f1 x = 1;
fun f2 (x, y) = (3 + s);
fun f3 (x, y) = 3 + s;
fun f4 (x, y) = (while 3 do s) * 3;

fun i x = if x then 1 else 2;

fun d x = f x;

fun fib n = if n == 1
            then 1
            else n * fib (n - 1);

fun e () = (f a) a;

fun s () = add (a, b);
